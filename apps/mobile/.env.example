# URL pública de la API desplegada (Render/Railway). Verifica el hostname real tras el
# primer deploy — Render le agrega un sufijo si el nombre "pulso-api" ya está tomado.
EXPO_PUBLIC_API_URL=https://pulso-api.onrender.com
